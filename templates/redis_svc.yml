apiVersion: v1
kind: Service
metadata:
  name: redis
  annotations:
    {{ if .Values.enableArgoCDHooks }}
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: "-1"
    {{ else }}
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-1"
    {{ end }}
spec:
  selector:
    app: redis
  ports:
    - port: 6379
